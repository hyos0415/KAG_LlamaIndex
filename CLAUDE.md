# CLAUDE.md

## 이 저장소의 두 가지 용도

**① 진단 (원래 목적)** — `docs/CONTEXT.md` 가 유일한 진실 원본이다. 작업 시작 전 읽어라.
기능 개발이 아니라 진단과 개선 측정이다. 금지 사항은 CONTEXT.md §7, 미정 사항은 §8.
미정 사항을 임의로 정하지 말고 물어라.

**② 다중 AI 협업 연습 + 신규 프로젝트 spec 스테이징 (2026-08-20 추가)** —
여러 표면(Cowork / GPT 데스크탑 / Codex / Claude Code)에서 나온 아이디어를 하나의
진실로 수렴시키는 절차를 연습하는 환경이다. 아래 §인박스 규칙을 따른다.

`docs/spec/` · `docs/review/` 는 신규 저장소로 이관될 초안이다
(`docs/handoff/finance_rule_graph_project_handoff.md` §13). 이 저장소를 새 프로젝트로
리팩토링하는 것이 아니다.

---

## 인박스 규칙

```
IDEA_CLAUDE/     Cowork 만 쓴다          ─┐
IDEA_GPT/        GPT 데스크탑/Codex 만 쓴다 ─┤ 진실 아님
                                          │
IDEA_RESOLVED/   Claude Code 만 쓴다      ─┘ 여기부터 진실
docs/            Claude Code 만 쓴다        명세·문서
```

### 1. 쓰는 주체를 넘지 않는다

`IDEA_CLAUDE/` · `IDEA_GPT/` 에 **쓰지 않는다.** 읽고 승격만 한다.
디렉토리마다 쓰는 주체가 하나여야 동시 쓰기 충돌이 구조적으로 없어진다.

### 2. 인박스는 근거가 아니다

`IDEA_*/` 의 내용을 `docs/` 나 결론에서 근거로 인용하지 않는다.
`IDEA_RESOLVED/` 로 승격된 뒤에만 인용 가능하다.

### 3. 세션 시작 시 인박스를 커밋한다

`IDEA_CLAUDE/` · `IDEA_GPT/` 에 새 파일이나 변경이 있으면 **작업 전에 먼저 커밋한다.**
사용자가 앱에서 같은 파일을 계속 덮어쓰는 것이 정상 사용 패턴이므로, 커밋되지 않은
버전은 그냥 사라진다. 커밋 메시지는 `[inbox] <출처> — <주제>` 형식.

지시 없이 자동으로 한다.

### 4. 승격 절차

`IDEA_RESOLVED/README.md` 의 5단계를 따른다. 요약:

1. 출처 머리글 정규화 (없으면 대화 맥락에서 복원)
2. 파일명 `YYYY-MM-DD-<주제-슬러그>.md` 로 정규화
3. handoff §16 4항목(Decision · Evidence · Alternative · Why rejected) + 반증 조건으로 변환
4. 원문은 인박스에 남긴다. 삭제하지 않고 경로를 출처에 적는다
5. **승격 직후 커밋한다. 예외 없다**

### 5. 내용 충돌은 사람이 정한다

두 앱이 같은 주제에 다른 결론을 냈으면 임의로 한쪽을 고르지 않는다.
양쪽을 나란히 제시하고 물어라. (CONTEXT.md §7.8 과 같은 원칙)

### 6. 워크트리는 상시로 쓰지 않는다

워크트리는 **독립 검증이 필요한 1회성 작업**에만 만든다
(예: 두 도구가 서로를 보지 않고 같은 대상을 감사할 때 — `docs/audit/` 이 그 사례).
끝나면 제거한다. 상시 작업을 워크트리에 나누면 같은 파일을 서로 다른 체크아웃에서
보게 되어 규칙이 깨진다.
