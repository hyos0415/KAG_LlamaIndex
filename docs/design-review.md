# KAG_LlamaIndex 진단 검토 — 설계 리뷰 (골격, 작성 중)

> 이 문서는 `docs/CONTEXT.md`의 요약본이 아니라 최종 산출물이다. 세부 수치·근거는 항상 `docs/CONTEXT.md`와 `tests/fixtures/*.json`을 원본으로 한다.
> 상태: 골격 + §4 확정 (엔티티 정규화 표본 판정 완료, 2026-08-02). §7 결론은 아직 서술 보강 필요.

---

## 1. 검증하려던 가설과 배경

_(작성 예정)_

- 지식그래프(KG) 도입 근거는 "LLM 응답이 일관되지 않다"였고, 이 근거 자체는 한 번도 검증된 적이 없었다.
- 프로젝트는 완성된 뒤 방치돼 있었다 — 기능 추가가 아니라 진단이 이번 작업의 범위였다.
- 원래 계획했던 그래프 순회 실험(1홉 vs 다홉 비교)은 데이터 부족으로 불가 판정이 났다 — §2 참고.

---

## 2. 실험 설계 반복 기록 (§5)

_(작성 예정 — `docs/CONTEXT.md` §5 "폐기된 계획과 이유" 표를 그대로 가져오되 서술로 풀어쓴다)_

- A/B (KG 우회 vs 현재) → 구조화와 순회가 동시에 달라 원인 분리 불가
- A/B/C (flat 형식 통일) → 정보 범위와 선택 방법이 여전히 함께 변함
- A/B/C/D (1홉 vs 다홉) → 브릿지 3개, 대표 경로가 전부 동일 엔티티 경유. 서로 다른 케이스 8개 구성 불가
- 코퍼스 확장으로 A/B/C/D 살리기 → RSS는 과거 기사를 주지 않음. 주제 집중 수집이 범위 밖

세 번의 재설계 모두 같은 질문으로 교란이 발견됐다: **검증 대상을 제거해도 결론이 같아지는가?**

---

## 3. 측정 지점과 사전 등록 항목 (§6), 동결 커밋 해시

_(작성 예정 — 아래는 커밋 해시 기준 뼈대. 서술 보강 필요)_

| 측정 지점 / 사전 등록 | 확정 커밋 | 날짜 |
|---|---|---|
| v0 (역사적 참고값, 재현 불가로 강등) | `3ba9426` (`v0-pre-experiment` 태그) | 2026-02-06 |
| §8 미정 3 (게이트 판정 기준) 확정 | `1c2f338` | 2026-08-01 |
| §8 미정 4 (로컬 JSON 저장소 유지) 확정 | `fdee5fd` | 2026-08-01 |
| §8 미정 5 (LLM 모델 고정) 확정, v0' 신설 | `13d4408` | 2026-08-01 |
| §8 미정 6 (jit_builder.py 경로 고정) 확정 | `5f7b06b` | 2026-08-01 |
| v0' 측정 완료 | `b972de5` | 2026-08-01 |
| v1 실행 전 해석 규칙 사전 등록 | `4bc39b9` | 2026-08-01 |
| v1 측정 완료 | `ebf75d9` | 2026-08-01 |
| 게이트 해석 규칙 정정(1차, cross_doc_path_ratio 인플레이션) | `b81108a` | 2026-08-01 |
| §8 미정 1·2 확정(별칭 규칙/온톨로지 범위), §4 소거법 초안 | `3d46c3e` | 2026-08-01 |
| v2a 상하한(lo/hi) 사전 등록 | `4c3cf7f` | 2026-08-01 |
| v2 실행 전 지표 유효성·가설 B 판정 기준 사전 등록 | `f9db9f2` | 2026-08-02 |
| v2 4변형(v2a-lo/hi, v2b, v2c) 측정 완료 | `6b9f9e9` | 2026-08-02 |
| 표본 기반 중지 규칙 사전 등록(40개 표본) | `393d5b2` | 2026-08-02 |
| 표본 40개 판정 반영 + 두 변형(strict/loose) 측정, §4 확정 | _(다음 커밋)_ | 2026-08-02 |

---

## 4. 소거법 결과 (§4) — 확정

| 층 | 검증 방법 | 결과 |
|---|---|---|
| 규모 | v0'→v1, 문서 5→40건 | entities_per_doc −2.0%. 증권 12건 부분집합에서도 악화 → 배제 |
| 관계 정규화 | 활용형 규칙 시뮬레이션 + v2b 실측 | 374→358종(4.3%), rel_type_ratio 0.595→0.574 → 거의 무효 |
| 엔티티 정규화 | 표본 40쌍 수작업 판정 | 유의미한 별칭 0/40, 상한 14/193 추정 → **기각** |
| 자유 스키마 추출기 | — | **최종 원인 (소거 완료)** |

**엔티티 정규화가 기각된 근거**: 직접 substring 교차문서 쌍 193개 중 고정 시드(42)로 뽑은 40개를 수작업 판정한 결과, 유의미한 별칭(표기 변형)은 0건이었다. "동일 대상"으로 판정된 4건은 전부 날짜·숫자 토큰(`5일`=`2월 5일`, `2026`=`2026년`, `5288`=`5288.08` 등)이며 실제 인물/기관 별칭이 아니다. 95% 신뢰 상한(rule of three)으로 193개 전체에 최대 14개까지 있을 수 있다고 잡아도, v2a-lo에서 실측한 leverage(5쌍→컴포넌트 1개 감소)를 적용하면 224개 컴포넌트 중 한 자릿수 감소에 그친다 — 사전 등록한 20% 중지 임계값에 크게 못 미친다.

이로써 자유 스키마 추출기가 그래프 조각화의 최종 원인으로 확정된다: 모델 세대를 올려도 타입 폭발이 유지되고(발견 6), 문서가 늘어도 어휘가 수렴하지 않으며, 엔티티 목록 자체가 고유명사가 아니라 서술구로 오염돼 있어(발견 14, 17, 18) 정규화 시도 자체가 애초에 손댈 대상을 찾지 못한다.

---

## 5. 사전 등록 기준의 3회 정정 이력 (발견 16)

_(작성 예정 — 무엇이, 언제, 왜 틀렸는지, 그리고 왜 그게 기록 가능했는지)_

1. **v0'→v1 해석 규칙**: cross_doc_path_ratio를 규모 효과의 확인 지표로 등록했으나, 조합론적으로 문서 수(정확히는 문서 쌍 수)에 비례해 인플레이션된다는 성질을 간과. v1 측정 후(`b81108a`) 정정 — entities_per_doc만 유효 지표로 재확정.
2. **v2 판정 기준**: components_per_doc 감소율로 가설 B를 판정하도록 사전 등록했으나, 노드 병합이 정당성과 무관하게 컴포넌트를 기계적으로 감소시킨다는 성질을 간과. v2 실행 후 판정을 무효화하고 무효 사유를 병기(기준 문구 자체는 보존).
3. **v2b 설계**: 관계 타입 정규화가 컴포넌트 구조에 영향을 줄 수 없다는 수학적 필연을 사전에 예측하지 못함.

세 경우 모두 기준이 날짜·커밋과 함께 문서에 남아 있었기 때문에 사후에 조용히 합리화하지 않고 무엇이 틀렸는지 특정할 수 있었다. **이것이 사전 등록의 가치다 — 설계를 옳게 만드는 것이 아니라 오류를 지목 가능하게 만드는 것.**

---

## 6. 대표 사례: 가짜 다리 (발견 17)

_(작성 예정)_

구준엽 부고 기사(11952002)와 국내 ETF 순자산 기사가 `5년`이라는 문자열 하나로 연결된 경로:

```
5년 → 2025년 펀드시장 결산 → 펀드시장 → 국내 펀드시장 순자산총액 → 순자산총액 → Etf 순자산총액 → Etf
```

`2025년 펀드시장 결산` 같은 긴 서술구가 엔티티 목록에 그대로 등록돼 있었고, 그 안에 우연히 `5년`이 부분 문자열로 포함되면서 완전히 무관한 두 기사가 이어졌다. 연결은 의미가 아니라 문자 수준 우연이다. 상세 경로는 `docs/CONTEXT.md` §6 "전이 경로 추적" 및 `docs/cluster-review.md` 참고.

---

## 7. 결론과 후속 계획

_(작성 예정 — §4 확정을 반영한 최종 서술은 아직 작성하지 않음. 아래는 확정된 사실관계만)_

표본 40개 판정 결과 유의미한 별칭 0/40 → 외삽 효과 20% 미만 → **가설 B 기각 확정, 193개 전수 판정 생략**(§6 중지 규칙 그대로 적용). §4가 확정됨에 따라 소거법의 네 칸이 모두 채워졌다.

최종 결론 서술과 후속 계획은 이후 작성한다.

후속 작업(§9): v5(스키마 제약 추출기 재도입) 등은 이번 범위 밖.

---

## 금지 사항 (이 문서 작성 시 준수)

- "Neo4j가 무가치하다" 같은 과잉 일반화 주장 금지 — Neo4j는 이번 v0'~v4 범위에서 아예 쓰이지 않았을 뿐(§8 미정 4), 무가치 여부는 검증 대상이 아니었다.
- 통계적 유의성 주장 금지 — 표본 크기가 유의성 검정을 지지하지 않으며, 이 프로젝트의 지표는 전부 결정론적 그래프 쿼리 값이다(§6).
- 사전 등록하지 않은 지표를 사후에 추가하지 않는다.
